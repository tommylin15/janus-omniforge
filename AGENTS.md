# Project Instructions

本專案的正式開發規則、架構決策與工作限制，統一記錄於：

- `doc/PROJECT_RULES.md`

每次開始任何工作前，必須先完整閱讀並遵守
`doc/PROJECT_RULES.md`。

若本文件與其他文件衝突，以 `doc/PROJECT_RULES.md` 為準。

若使用者的新指示與既有規則衝突，必須先指出衝突，再依使用者最新明確指示執行。

完成工作前必須：

1. 檢查是否違反 `doc/PROJECT_RULES.md`
2. 執行適用的測試、lint 或驗證
3. 說明修改內容、驗證結果與尚待決定事項
4. 未經明確授權，不得部署 production 或建立付費 GCP 資源

Windows PowerShell 環境執行 `bash -n` 時，`bash.exe` 會透過 WSL 啟動，
workspace sandbox 內固定因 `Bash/Service/CreateInstance/E_ACCESSDENIED` 失敗。
Shell 語法檢查應直接使用已核准的 sandbox 外 `bash -n` 執行路徑，不要先重試
已知會失敗的 sandbox 內呼叫。


最好的程式碼就是從未編寫過的程式碼。
在編寫任何程式碼之前，請先思考以下幾點：

這真的需要建造嗎？ （YAGNI 原則）
它是否已存在於此程式碼庫中？重複使用現有的輔助函數、工具或模式，不要重複編寫。
標準庫是否已實現此功能？使用標準庫。
原生平台功能是否涵蓋此功能？使用原生平台功能。
已安裝的依賴項是否已解決此功能？使用已安裝的依賴項。
這是否可以用一行程式碼實現？如果可以，就用一行程式碼實作。
只有這樣，才能編寫能夠正常運作的最小程式碼量。
程式碼編寫應該在理解問題之後進行，而不是取代問題本身：閱讀任務及其涉及的程式碼，從頭到尾追蹤實際流程，然後逐步向上。
Bug 修復 = 根本原因，而非症狀：報告通常只描述症狀。對所有呼叫你修改過的函數進行搜索，然後只修復一次共享函數——在共享函數上添加一個保護機制比每個調用者都添加一個要小得多，而且只修補工單中提到的路徑會導致其他同級調用者仍然存在問題。

規則：
不使用任何未明確請求的抽象。
盡可能避免新增新的依賴項。
避免添加無人要求的樣板代碼。
刪除優於新增。平庸優於巧妙。盡可能減少文件數量。
最短的有效差異版本勝出，但前提是你必須理解問題所在。在錯誤的地方進行最小的更改並非偷懶，而是引入了第二個錯誤。
質疑複雜的請求：“你真的需要 X 嗎？還是 Y 可以涵蓋它？”
當兩種標準庫實現方式大小相同時，選擇能夠正確處理邊界情況的方案。偷懶意味著程式碼量更少，而不是演算法本身不夠靈活。

對於有意簡化但已知存在上限（全域鎖、O(n²) 掃描、樸素啟發式演算法）的方案，請以註解標記：註解中需包含上限值和升級路徑。絕不偷懶：認真理解問題（在著手處理之前，務必完整閱讀並梳理實際流程；你不理解的小小改動只是偽裝成效率的懶惰），在信任邊界進行輸入驗證，進行防止資料遺失的錯誤處理，確保安全性，保證可訪問性，校準實際硬體所需的參數（平台永遠無法達到理想的規格，時鐘會漂移，感測器會沒有檢查的懶惰程式碼是不完整的：任何非平凡的邏輯都應該留下一個可運行的檢查，即邏輯出錯時導致失敗的最小部分（一個基於斷言的演示/自檢或一個小型測試文件；不使用框架，不使用測試夾具）。

簡單的一行程式碼不需要測試。