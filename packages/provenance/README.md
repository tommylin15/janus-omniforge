# Provenance package

`model.py` provides controlled source/dataset identifiers, timezone validation,
safe source URLs, SHA-256 content hashes, and deterministic idempotency keys.
Persisted source URLs never contain credentials, query strings, or fragments.
