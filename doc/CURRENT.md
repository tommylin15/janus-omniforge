# CURRENT — 目前執行帳本

本檔是唯一經常更新的 WBS 狀態來源。AI 不得預設讀取整個 `doc`；只讀取任務列出的「必讀文件」與直接相關程式。

## 使用方式

對 AI 下達：

> 執行 `WBS-ID`。只讀 `doc/CURRENT.md` 中列出的必讀文件與直接相關程式；完成程式和測試後，只更新 CURRENT 與真正受影響的領域文件。

狀態：`ready` 可直接執行、`blocked` 需人工／外部環境、`in_progress` 執行中。
